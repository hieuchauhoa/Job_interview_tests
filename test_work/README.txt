*Link demo: https://youtu.be/r7JDMyTptfY
Cách cài đặt và chạy các chương trình:

Câu 1:

a/
- Mở file Cau1_a.cpp ở thư mục test_work\Cau1_a.
- Thay đổi chuỗi input ở mảng "array". 
- Và sau đó chạy file.

b/ 
- Mở file Cau1_b.html bằng các phần mềm viết code (ví dụ: Visual Studio Code)
- Chỉnh sửa input tại biến "arr1" tại dòng code thứ 5.
- Sau đó chạy file Cau1_b.html bằng trình duyệt để xem kết quả.

Câu 2:

Sử dụng công cụ Xampp để chạy file php và tạo database.

-Đầu tiên ta tạo database có tên "form" và import file "form.sql" để tạo bảng.
-Di chuyển thư mục "test_work" vào thư mục htdoc của Xampp.
-Vào trình duyệt mở file "index.php" theo đường dẫn http://localhost/test_work/cau2/php.
-Nhập dữ liệu và vào database kiểm tra.

